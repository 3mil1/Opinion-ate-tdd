import React from "react";
import RestaurantList from "./RestaurantList";

export const RestaurantScreen = () => {
    return (
        <div>
            <h1>Restaurants</h1>
            <RestaurantList/>
        </div>
    )
}
